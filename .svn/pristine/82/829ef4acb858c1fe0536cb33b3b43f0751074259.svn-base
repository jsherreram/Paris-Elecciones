(function () {
    angular.module('app').config(function ($routeProvider) {
        $routeProvider.when("/", {
            templateUrl: "templates/ConciliacionPagosNomina.html",
            controller: "ConciliacionPagosNomina as controller"
        }).otherwise({redirectTo: "/"});
    });
})();
