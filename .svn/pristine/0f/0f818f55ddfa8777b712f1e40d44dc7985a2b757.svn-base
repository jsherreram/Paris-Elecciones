(function () {
    angular.module('app').config(function ($routeProvider) {
        $routeProvider.when("/", {
            templateUrl: "templates/generaCarnet.html",
            controller: "generarCarnetIndividual as controller"
        })
        .otherwise({redirectTo: "/"});
    });
})();
