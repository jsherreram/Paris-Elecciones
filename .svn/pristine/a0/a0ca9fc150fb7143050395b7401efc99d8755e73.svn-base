(function () {
    angular.module('app').config(function ($routeProvider) {
        $routeProvider.when("/", {
            templateUrl: "templates/seleccionarArchivo.jsp",
            controller: "generarArchivo as controller"
        })
        .otherwise({redirectTo: "/"});
    });
})();
