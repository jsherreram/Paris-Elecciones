(function () {
    angular.module('app').config(function ($routeProvider) {
        $routeProvider.when("/", {
            templateUrl: "templates/notificacionPago.html",
            controller: "NotificacionPago as controller"
        }).otherwise({redirectTo: "/"});
    });
})();
